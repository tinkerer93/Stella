language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
install:
  - pip install pipenv
  - pipenv install --dev
  - pip install pytest-xdist
script:
  - pytest -d --tx popen//python=python[ $TRAVIS_PYTHON_VERSION ]
notifications:
  email:
    on_success: never
    on_failure: change

